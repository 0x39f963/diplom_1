# ----- eva-agent-lab - зависимости (Python 3.12) -----
# Граф / агенты
langgraph>=0.2.40
langgraph-checkpoint>=2.0.0
langchain-core>=0.3.0

# LLM-коннекторы (OpenRouter + локальный Ollama, оба через OpenAI-совместимый SDK)
openai>=1.40.0
httpx>=0.27.0

# Контракты / настройки
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.0
mcp>=1.10,<2

# Поиск по закону - отдельный сервис; в агенте только httpx-клиент, ML-веса не тянем

# Защитный слой
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
confusable-homoglyphs>=3.3.1
# spaCy RU-модель ставится отдельно: python -m spacy download ru_core_news_lg

# Observability
langfuse>=2.50.0

# Демо-интерфейс + CLI
chainlit>=1.1.0
rich>=13.7.0

# Dev / гейты
ruff>=0.6.0
mypy>=1.11.0
pytest>=8.3.0
